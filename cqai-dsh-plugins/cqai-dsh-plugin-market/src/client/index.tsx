/**
 * The complete market surface is owned by dsh-community-market. This client
 * entry intentionally stays empty so CQAI does not create a second, reduced
 * market tab beside the full storefront.
 */
export const inject: readonly string[] = []

export function apply(): void {}
